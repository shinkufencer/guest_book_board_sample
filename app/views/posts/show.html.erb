<div class="container mx-auto px-1">
  <p id="notice"><%= notice %></p>

  <div class="py-3">
    <div class="font-bold">
      [<%= @post.id %>]<%= @post.title %> - <%= @post.user.name %>さん
    </div>
    <div class="py-1">
      <%= simple_format(@post.message) %>
    </div>
  </div>
</div>
