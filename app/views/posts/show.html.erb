<div class="container mx-auto px-1">
  <p id="notice"><%= notice %></p>

  <div class="py-3">
    <div class="font-bold">
      [<%= @post.id %>]<%= @post.title %> - <%= @post.user.name %>さん
    </div>
    <div class="py-1">
      <%= simple_format(@post.message) %>
    </div>
  </div>
</div>
<%= render 'new_arrival_top3', new_arrival_posts: @new_arrival_posts %>
<%= render 'relation_post', relation_posts: @relation_posts %>
<%= render 'recommend_post', recommend_posts: @recommend_posts %>